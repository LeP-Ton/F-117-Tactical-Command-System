## AI DEBUG切换功能的设计问题
当前开启AI DEBUG，能观察到雷达旋转、追踪热力图等地图的完整信息，有利于玩家直播演示，但关闭AI DEBUG有利于玩家进行解谜，需要思考二者如何取舍或融合

## 指挥官性格：历史作用、当前方案与未来设计

### 1. 旧版 Commander Doctrine（指挥官性格）

旧版由 Mission Seed 在以下四种 Doctrine 中确定一种，相同 Seed 可以复现：

- `Conservative`（谨慎型）
- `Aggressive`（进攻型）
- `Ambush`（伏击型）
- `Analytical`（分析型，早期默认值）

旧版性格主要通过修改 Commander 的 Utility（效用）评分，影响 `MONITOR`、`COORDINATED_SEARCH`、`CONCENTRATE_SEARCH` 和当时存在的 `NETWORK_SILENCE` 选择。`Ambush` 还会在开局短暂进入网络静默，之后恢复搜索。Final Strike 曾根据失败记录、Enemy Alert 和玩家直达航线倾向切换 Doctrine，高警戒最终战可能转为 `Aggressive`。

该设计原本希望让相同地形、天气和雷达布局拥有不同的防空反应风格，增加 Seed 地图差异和 Roguelike 重玩价值。但实际存在以下问题：

- 差异主要是隐藏的 Utility 加减分，玩家难以观察和学习。
- 战前预览虽然显示 Doctrine 名称，但没有清楚解释它会怎样改变雷达行为。
- 多种性格没有形成稳定、明显且可利用的弱点。
- `Ambush` 强依赖后来被删除的雷达静默与网络静默机制。
- Doctrine 与 Awareness、雷达模式、指挥链等机制叠加后，提高了理解成本。

因此旧版 Doctrine 在核心系统简化时被完整移除，包括类型字段、Seed 生成、Campaign 预览、Utility 偏置、Final Strike 切换与 UI 显示。历史变更文档记录了删除范围，但没有保留四种性格的全部精确评分参数，未来不应机械恢复旧实现。

### 2. 当前无性格 Commander 方案

当前所有任务使用同一套中性 Commander 逻辑：

- 只读取 Awareness、Belief Map 和雷达状态，不读取飞机真实位置。
- Awareness 决定整体搜索强度。
- 有效 Belief/CMD 决定搜索方向；失去 Belief 后不会使用目标区或地图边缘作为定位回退。
- Commander 在 `MONITOR`、`COORDINATED_SEARCH`、`CONCENTRATE_SEARCH` 三种意图之间进行 Utility 决策。
- Commander 通过偏置引导各雷达选择 `WIDE_SEARCH`、`SECTOR_SEARCH` 或 `FOCUSED_TRACK`。
- 指挥链效率影响决策延迟、Contact 共享窗口、联合火控和搜索方位误差。

当前方案的优点是规则统一、容易调试，玩家可以把注意力集中在 Contact → Belief → 搜索 → 跟踪 → 锁定 → 导弹这一条核心链路上。缺点是不同 Seed 主要依赖地形、天气和雷达部署产生差异，敌方指挥行为长期游玩后可能趋同。

### 3. 推荐的未来方案：可读的防空指挥风格

指挥官性格可以恢复，但应作为“地图防空特征”重新设计，不恢复旧版隐藏评分偏置，也不恢复雷达静默。建议只保留三种风格：

#### 谨慎型

- Contact 出现后优先扩大扇区搜索，不急于集中全部雷达。
- 进入联合跟踪和集中搜索较慢。
- 优势：搜索覆盖持续且不容易被单一诱导方向牵制。
- 弱点：形成高质量锁定较慢，玩家有更长脱离窗口。

#### 进攻型

- 更早调集邻近雷达共享 Contact 并形成联合跟踪。
- 更快从扇区搜索升级为集中搜索。
- 优势：锁定压力高，发现后的威胁升级迅速。
- 弱点：容易让多个雷达集中到同一区域，其他方向覆盖下降。

#### 猎伏型

- 低置信度 Contact 出现时维持宽域或有限扇区搜索。
- Belief 达到可靠阈值后突然集中雷达追踪。
- 优势：威胁升级具有突发性，不容易通过短暂弱 Contact 判断真实反应。
- 弱点：确认目标前反应较慢，玩家可利用低置信度接触窗口穿越。

### 4. 新方案设计边界

- 风格由 Run/Mission Seed 确定，相同 Seed 必须复现。
- Campaign 节点预览必须显示风格名称和一句可行动的行为说明。
- 普通玩家视图需要通过搜索扇区、威胁升级速度或简短提示感知差异，不能依赖 AI DEBUG 数字。
- 每种风格只允许两到三个主要行为差异，避免重新堆叠隐藏参数。
- 风格可以改变 Awareness 阈值响应、Contact 共享规模、雷达调集数量和集中搜索保持时间。
- 风格不得直接修改底层 Sensor 探测概率，不得提高 Contact 的物理精度，也不得读取飞机真实位置。
- 不恢复 `SHUTDOWN`、`NETWORK_SILENCE` 或投弹目标区定位回退。
- 指挥链效率仍是统一的系统能力；性格决定“如何使用能力”，不能取代 CMD LINK 的作用。

### 5. 待办与验收条件

- [ ] 为三种指挥风格编写可量化行为表，明确 Awareness、Belief 置信度和失联时的状态转换。
- [ ] 确定每种风格最多三个差异参数，禁止仅为制造差异增加无解释评分项。
- [ ] 将风格接入 Seed 生成，并增加相同 Seed 复现测试。
- [ ] 在 Campaign 节点预览中显示风格名称、优势和可利用弱点。
- [ ] 在普通战术界面中提供不泄露真实位置的行为反馈。
- [ ] 为三种风格分别建立“发现前、首次 Contact、可靠 Belief、失联恢复”测试。
- [ ] 进行盲测：玩家不看 AI DEBUG 时，应能根据数局行为大致判断指挥风格。
- [ ] 对比无性格版本，确认风格确实改变航线决策，而不只是改变数值或难度。
- [ ] 确认三种风格都存在可利用弱点，没有单纯的最强或最弱选项。

在上述验收条件满足前，当前中性 Commander 继续作为稳定基线。

## 低价值与冗余设计清理计划

### 判断标准

- 玩家是否能感知并据此改变航线或 Campaign 选择。
- 状态是否可以从唯一事实来源派生，是否存在双写和同步风险。
- 复杂度是否只服务 DEBUG 展示，而没有改变正式玩法。
- 同名或异名机制是否最终只执行同一种数值运算。

### 已完成：第一批确定性清理

- [x] 结构化事件只在 AI DEBUG 中显示，正常视图不再泄露 Belief、Commander 或 Operator 内部活动。
- [x] 删除高频 `BELIEF_UPDATED` 事件。
- [x] 任务事件限制为最近 200 条，音频改为按事件 ID 消费。
- [x] 删除无消费方的 `missionHistory` 与永不产生的 `ABORTED`。
- [x] 删除可从节点状态派生的 `completedNodeIds`。
- [x] 删除可从数组长度派生的 `generationInfo`。
- [x] 删除独立 `adaptationLevel`，改由有效轨迹样本数派生。
- [x] 删除 `modeChangedAt`、`lastContactAt`、`lastDecisionAt` 与 `launches` 等不影响行为的只写字段。
- [x] 删除正常界面的 `ARCHITECTURE STATUS / RUN ≠ MISSION` 开发说明。
- [x] 修正旧 Intel 文案和 `7.2 u/s` 过时记忆。

### 已完成：第二批消除伪选择

- [x] 将 `RECON/ELINT` 合并为 `INTEL`。
- [x] 将 `STRIKE/DEEP_STRIKE` 合并为 `STRIKE`。
- [x] Campaign 改为三个顺序二选一阶段与 Final Strike。
- [x] 执行节点后同层另一节点进入 `EXPIRED`，再解锁下一阶段。
- [x] 普通任务失败仍推进并增加 Enemy Alert；飞机被摧毁仍立即结束 Run。

### 已完成：第三批合并同构机制

- [x] 撤销 Terrain 与 Weather 的统一领域模型：Terrain 保持静态，Weather Cell 独立保存移动、强度、尺度与类型演化参数。
- [x] Weather 扩展为 Cloud、Rain、Storm、Fog，并按 Seed 与任务绝对时间动态改变位置、范围、强度和类型。
- [x] 增加带 Seed 固定误差的 `T+30/60/90s` 天气预报、可信度和规划地图预测轮廓。
- [x] 正常雷达情报只显示可信等级，不再同时展示精确可信度百分比。
- [x] Enemy Adaptation 改为分析按实际位移采样的飞行轨迹，不再依赖航点密度。
- [x] 敌方反制按目标位置选择最近且未使用的雷达，不再固定修改数组第 1/2/3 项。

### 已完成：F-117 燃油航程限制

- [x] 满油航程固定为 `2000 u`，等于当前地图两条边的总长度。
- [x] 按自动驾驶实际累计飞行距离扣减燃油，转弯和单 Tick 多航段不会少计。
- [x] 剩余燃油不足一帧航程时限制实际移动距离，耗尽后停止并令当前任务失败。
- [x] 右侧遥测显示剩余航程、燃油百分比与低于 20% 的红色告警。

### 已完成：天气飞行减速

- [x] Cloud、Fog、Rain、Storm 分别造成 10%、15%、20%、30% 飞行减速。
- [x] 多个天气重叠时取最强减速，避免倍率连乘导致异常低速。
- [x] 燃油继续按真实飞行距离消耗，天气只改变飞行时间和暴露时长。
- [x] 飞行遥测显示当前有效速度与天气减速百分比。

### 明确保留

- [x] 保留 Radar Operator 三种扫描模式，它们会真实改变扫描线行为。
- [x] 保留 Awareness 与 Engagement 两种时间尺度：前者驱动全局搜索，后者驱动火控与导弹。
- [x] 保留 Contact → Belief → Commander 信息边界，后续 AI 仍禁止读取飞机真实位置。

### 暂缓：Belief Map 简化实验

- [ ] 实现仅用于对照测试的 `TrackEstimate`，包含位置、速度、置信度和失联年龄，不立即替换正式模型。
- [ ] 使用固定 Seed 批量记录 Commander 搜索方位、Operator 模式序列、Contact 连续时间和任务胜负。
- [ ] Track Estimate 与 24×24 网格在至少 95% 场景行为等价时，才允许替换正式 Belief Map。
- [ ] 未达到 95% 时保留网格，并记录造成差异的场景，不通过调参强行制造等价。

### 备选方案与排除依据

- 删除全部 Commander/Belief/Operator：会破坏敌方只能消费不完美 Contact 的核心特色，排除。
- 保留所有结构只调整数值：无法解决伪选择、重复状态、信息泄露和无界事件增长，排除。
- 当前采用“先删确定无价值表示，再合并同构玩法，最后用对照实验决定 Belief”的渐进方案。
