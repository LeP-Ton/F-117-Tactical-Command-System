# 任务区域重构文案审计

## 背景与目标

- 审查雷达、飞机起点、撤离区和打击目标生成范围重构后，玩家界面与现行文档是否仍引用旧坐标、旧半径或旧净空规则。
- 保留真实历史记录，同时避免根索引把已废止规则误导为当前实现。

## 审计结论

- 中英文任务引导、README、机制手册与 `AGENTS.md` 已使用当前规则，没有遗留“固定东北撤离区”、`100 u` 目标攻击半径或撤离区 `80 u` 雷达净空文案。
- 游戏内文案继续使用“本次任务的撤离区”“目标攻击圈”等沉浸式表达，不直接展示精确生成参数；数值规则仍由中英文机制手册统一维护。
- 根索引中两条旧 workflow 的读取说明仍可能让后续开发误以为旧规则有效，已改为明确的历史规则标记，并指向会话-17至19的最终规则。

## 当前规则基线

- 玩家坐标原点位于左下角，飞机起点为 `(100,100)`。
- 撤离区为 `100×100 u`，中心从 `(100,900)`、`(900,900)`、`(900,100)` 中确定性选择。
- 目标中心双轴范围为 `300–700`，攻击半径为 `50 u`。
- 雷达中心双轴范围为 `200–800`，不执行飞机起点或撤离区净空。

## 变更范围

- `.agentdocs/index.md`
  - 将旧固定东北撤离区条目标为已废止历史规则。
  - 将旧撤离区 `80 u` 雷达净空条目标为已废止历史规则。

## 风险与验证

- 未改写历史 workflow、旧存档兼容测试或迁移夹具，避免破坏规则演进记录与兼容性覆盖。
- 使用全文检索核对现行中英文文案与文档中的旧方位、旧半径、旧坐标范围和旧净空描述。
- 本次仅修改文档导航，不改变运行时代码。
