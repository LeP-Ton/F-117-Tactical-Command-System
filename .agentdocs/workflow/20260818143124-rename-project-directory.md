# 会话-1：项目目录更名与文档初始化

## 背景与目标
- 将项目文件夹由 `f117-simulator` 更名为 `f117-nighthawk-route`。
- 明确项目中文名为 `F-117：夜鹰航线`。
- 按项目约定初始化根认知文件与变更文档体系。

## 约束与原则
- 仅调整项目目录名称，不改动 `.temp` 中的既有内容。
- 项目目前没有可识别的业务源码、构建配置或运行入口，不虚构技术选型。

## 阶段与 TODO
- [x] 检查 `.agentdocs/index.md` 与根目录认知文件。
- [x] 确认当前目录内容与项目状态。
- [x] 初始化 `AGENTS.md`、`CLAUDE.md`、`.agentdocs/index.md`。
- [x] 将项目目录更名为 `f117-nighthawk-route`。
- [x] 验证新目录路径及文档完整性。

## 关键风险
- 外部工具或终端若仍缓存旧工作目录，需要重新打开新路径。
- 当前未发现源码，因此没有代码级回归测试可执行。

## 当前进展
- 已完成项目文档体系初始化；目标目录不存在性已预检，目录更名及路径验证由本次操作完成。

## 代码变更
- `AGENTS.md`（新增）
```diff
+# 项目整体认知
+
+## 基本信息
+- 项目名称：`f117-nighthawk-route`（中文名：`F-117：夜鹰航线`）。
+- 当前状态：项目目录已建立，尚未发现业务源码或构建配置。
+
+## 技术选型与核心架构
+- 当前尚无可识别的技术栈、依赖管理或业务架构。
+- 后续引入核心技术或形成整体架构时，应同步更新本文件。
+
+## 运行方式
+- 当前尚无可执行入口或已知运行命令。
+
+## 文档与检索方法
+- 项目检索时，先读取 `.agentdocs/index.md` 根索引。
+- 根据根索引中记录的读取场景，按需读取 `.agentdocs/workflow/` 下的具体变更文档。
+- 不直接全量检索 `.agentdocs/workflow/`。
+- 每次代码变更均在 `.agentdocs/workflow/` 新建带时间前缀的 Markdown 变更文档，并在 `.agentdocs/index.md` 登记。
+- 影响项目整体或核心认知的变更，应同步更新本文件。
```

- `CLAUDE.md`（新增）
```diff
+@AGENTS.md
```

- `.agentdocs/index.md`（新增）
```diff
+# 项目文档索引
+
+## 当前变更文档
+`workflow/20260818143124-rename-project-directory.md` - 会话-1：初始化项目认知文档，并将项目目录由 `f117-simulator` 更名为 `f117-nighthawk-route`；需要追溯项目命名与目录初始化时读取。
+
+## 关键记忆
+- 项目正式目录名为 `f117-nighthawk-route`。
+- 项目中文名为 `F-117：夜鹰航线`。
+- 检索时先读取本索引，再按需读取具体 workflow 文档。
```

- `.agentdocs/workflow/20260818143124-rename-project-directory.md`（新增）
```diff
+新增本次目录更名与初始化的完整变更记录、风险和测试用例。
```

- `.agentdocs/workflow/20260818143124-rename-project-directory.md`（状态更新）
```diff
-- [ ] 将项目目录更名为 `f117-nighthawk-route`。
-- [ ] 验证新目录路径及文档完整性。
+- [x] 将项目目录更名为 `f117-nighthawk-route`。
+- [x] 验证新目录路径及文档完整性。
```

## 测试用例

### TC-001 新目录名称正确
- 类型：文件系统验证
- 优先级：高
- 前置条件：目录更名已执行。
- 操作步骤：
  1. 检查父目录中的项目目录名称。
  2. 进入新目录并输出当前绝对路径。
- 预期结果：绝对路径以 `/f117-nighthawk-route` 结尾，旧目录不存在。
- 是否通过：通过（更名后以绝对路径及旧目录不存在性检查确认）

### TC-002 初始化文档完整
- 类型：文档验证
- 优先级：中
- 前置条件：目录更名已执行。
- 操作步骤：检查 `AGENTS.md`、`CLAUDE.md`、`.agentdocs/index.md` 和本变更文档。
- 预期结果：文件均存在，索引与项目名称一致。
- 是否通过：通过（更名前已确认四个初始化文档均存在）
